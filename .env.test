# Server Configuration
PORT=5001
NODE_ENV=test
API_URL=http://localhost:5001/api

# Database Configuration
MONGODB_URI=mongodb://localhost:27017/freelancer-platform-test

# JWT Configuration
JWT_SECRET=test-jwt-secret-key-123
REFRESH_TOKEN_SECRET=test-refresh-token-secret-key-123

# Email Configuration (use test service in test environment)
EMAIL_HOST=smtp.ethereal.email
EMAIL_PORT=587
EMAIL_USER=test@ethereal.email
EMAIL_PASS=testpassword

# AWS Configuration (use local storage in test environment)
AWS_BUCKET_NAME=test-bucket
AWS_ACCESS_KEY_ID=test-access-key
AWS_SECRET_ACCESS_KEY=test-secret-key
AWS_REGION=us-east-1

# Redis Configuration (use mock in test environment)
REDIS_URL=redis://localhost:6379/1

# Payment Gateway Configuration (use test mode)
STRIPE_SECRET_KEY=sk_test_your_stripe_key
STRIPE_WEBHOOK_SECRET=whsec_test_your_webhook_secret

# OAuth Configuration (use test credentials)
GOOGLE_CLIENT_ID=test-google-client-id
GOOGLE_CLIENT_SECRET=test-google-client-secret
GITHUB_CLIENT_ID=test-github-client-id
GITHUB_CLIENT_SECRET=test-github-client-secret

# Other Configuration
CORS_ORIGIN=http://localhost:3000
RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX=100
FILE_UPLOAD_MAX_SIZE=5242880
SESSION_SECRET=test-session-secret-123

# Feature Flags
ENABLE_SOCKET_IO=false
ENABLE_RATE_LIMITING=false
ENABLE_REQUEST_LOGGING=false
ENABLE_RESPONSE_COMPRESSION=false
ENABLE_SSL=false
